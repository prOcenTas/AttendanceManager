<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidattendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAttendance">
        <activity android:name=".Activitys.UserActivity"></activity>
        <activity
            android:name=".Activitys.LoginActivity"
            android:theme="@style/Theme.AndroidAttendance.NoActionBar" />
        <activity
            android:name=".User.RegisterUser"
            android:theme="@style/Theme.AndroidAttendance.NoActionBar" />
        <activity android:name=".Activitys.MainActivity"
            android:theme="@style/Theme.AndroidAttendance.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>